services:
  - type: web
    name: raavansh-hotel
    runtime: node
    plan: free
    buildCommand: echo "no build needed - pure Node, zero dependencies"
    startCommand: node server.js
    healthCheckPath: /
